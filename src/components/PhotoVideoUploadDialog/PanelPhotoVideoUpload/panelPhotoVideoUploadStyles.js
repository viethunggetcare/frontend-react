const styles = (theme) => {
  return {
    root: {
      position: 'relative',
    },
    photoVideoList: {
      width: 'calc(100% * 6 / 12)',
      marginLeft: 'calc(100% * 6 / 12)'
    }
  }
}

export default styles;