export default (theme) => {
  return {
    thumbnail: {
      marginRight: theme.spacing(2),
      width: theme.spacing(5),
      height: theme.spacing(5),
    },
    title: {
      flex: 1,
    }
  }
}