const styles = (theme) => {
  return {
    containedCustom: (props) => {
      const styles = {};
      // switch (props.color) {
      //   case 'secondary':
      //     Object.assign(styles,{
      //       background: theme.palette.backgroundRadialGradient.secondary,
      //       color: '#FFFF',
      //     })
      //     break;
      //   default:
      //     Object.assign(styles,{
      //       background: theme.palette.backgroundRadialGradient.primary,
      //       color: '#FFFF',
      //     })
      //     break;
      // }
      return styles;
    }
  }
};


export default styles