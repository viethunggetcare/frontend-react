const styles = (theme) => {
  return {
    root: {
      width: 320,
      maxWidth: `calc(100vw - 15%)`
    },
    title: {
      fontWeight: 600,
      marginBottom: theme.spacing(1)
    }
  }
}

export default styles;